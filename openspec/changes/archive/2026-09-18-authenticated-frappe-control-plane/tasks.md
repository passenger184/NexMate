# Tasks: authenticated-frappe-control-plane

**Approval and evidence update — 2026-09-17:** The user approved implementation once plans were internally consistent and strictly validated. The initial handoff updated README.md, DEVELOPMENT.md and progress records; the earlier continuation updated only progress/CURRENT.md, progress/JOURNAL.md and this task record. The acceptance completion updates DEVELOPMENT.md and those three files only. Original task wording below is retained; references to the planning pass describe its earlier scope. Checked tasks reflect inspected current source/tests plus the user-supplied offline execution report, not runtime tests rerun here or final independent acceptance. The four user choices are resolved in design.md D0; remaining HLD decisions are not selected.

**Evidence:** HEAD `3e060c3` plus uncommitted implementation; supplied first full pass on 2026-09-17, before the wording fix: project `.venv` discovery 190 tests OK with dotenv disabled and model/network integrations mocked; Node harness: 64 checks (26 preview + 38 Desk); syntax, strict OpenSpec and diff checks reported passing. Preliminary system `python3` had 13 import failures, superseded by the successful `.venv` run. Source anchors and limitations are recorded in progress/CURRENT.md. Authentication matrix, gateway/envelope/dispatch tests and stub-DOM checks support the checked implementation tasks; they do not prove real Bench authentication/CSRF, installation or assets.

**Latest parent-reported evidence — 2026-09-17:** Pre-runtime strict validation was independently confirmed, satisfying 1.1. Final read-only security review found no blocking finding; OpenSpec verification mapped 18 requirements and 51 scenarios with incomplete-acceptance warnings. The last clarification/out-of-scope wording warning was fixed with scope-aware guidance and the additional regression test at `tests/test_chat_boundary.py:236`. The parent reports the full project `.venv` rerun exited OK after the fix and the Node harness passed. No exact latest executed count is inferred; 190 Python / 64 Node above is the historical first full pass. These bounded reviews are complete, not overall acceptance; no runtime tests were rerun here.

**Acceptance-session update — 2026-09-17:** User-supplied evidence: offline `PYTHON_DOTENV_DISABLED=1 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 LITELLM_LOCAL_MODEL_COST_MAP=True .venv/bin/python -m unittest discover -s tests` returned 191 tests OK in 1.628s with a Starlette/httpx deprecation warning and no dependency changes; Node harness 64 checks pass (26 preview + 38 Desk); `node --check` passes for bundle and harness. Tooling inspection (root/nested pyproject/package, Makefiles, tasks, CI, scripts, docs): Python lint/typecheck and JS lint/typecheck each **not configured**; nested `frappe_app/pyproject.toml` is packaging only; `.opencode` deps are agent tooling, not app tooling. Per explicit user acceptance, 6.1 is complete as not-configured (not passed lint). Bench inspection found `/home/passenger/projects/frappe_docker/development/frappe-bench` with apps crm/erpnext/frappe/hrms and `sites/development.localhost`, but no `erpnext_ai_copilot` app directory; `bench`, `chromium`, `google-chrome` absent from PATH; `ss` showed only DNS listeners; bounded no-proxy curl to `http://127.0.0.1:8081` exited 7 (connection refused, HTTP 000); `docker ps`/`version`/`compose` could not execute `/usr/bin/docker` (Input/output error), so container status is undetermined from CLI, not evidence of no containers globally. No install/start/config/migration/login/browser/model/ERP-data actions. The operator shell read the adjacent `frappe_docker` `.env` via name-filtered output (nonsecret values visible, not reproduced); the target repo's `.env` was not read; no credentials used/changed. The prior read-only security review (no blocking finding) and latest independent final scope review (no out-of-scope runtime/dependency work) are not rerun. The user explicitly authorizes the task-7.3 handoff ONLY despite pending Bench verification: the separately approved `frappe-owned-conversation-state` successor must own state (authenticated user/site, persistence across inference restarts), remove caller-controlled session ownership, define explicit migration/compatibility and eventually remove the JSON store; nothing is scaffolded or implemented. **Task status: 20/21 complete; only 6.3 remains open.**

**Open gates:** Only 6.3 (real authorized Bench/Desk integration evidence) remains open; 6.1 is closed as not-configured and 7.3 closed as authorized handoff-only. No final boundary acceptance, full G2/M2, production readiness, state ownership, ACL isolation, release/write approval or cloud consent is claimed. Task 6.4 is source/test-based rollback review, not a deployed rotation/rollback exercise.

## 1. Confirm bounded implementation contract

- [x] 1.1 At implementation handoff, review the appended 2026-09-17 ADR clarification against proposal/design/all three specs: explicit role mapping, transitional sessions, chat-only backend enforcement, minimal public health, dual development opt-in and single-project binding. Verify consistency and strict OpenSpec validation before implementation; do not rewrite historical ADRs or treat this as full G2/M2 acceptance.

## 2. Inference service authentication

- [x] 2.1 Implement configuration for `NEXMATE_SERVICE_KEY`, `NEXMATE_ENV` (default production), `NEXMATE_DEV_UNAUTHENTICATED` (default off, only `1` enables), and fixed `NEXMATE_FRAPPE_SITE`. Validate strong 64-hex-character key representation, safe configuration errors and exact environment semantics. Verify every D2 truth-table case with synthetic configuration; these implementation-level names do not select topology.
- [x] 2.2 Implement shared-secret gate with constant-time byte comparison. Exact `/health` is always exempt and returns fixed minimal liveness only, even with missing/invalid key. Gate all other routes/assets/docs/unknown paths without OPTIONS or prefix exceptions. Verify missing/invalid/duplicate headers, unset/invalid keys, production/default/unknown environments and both development switches, including unset-key development with no header and refusal of a supplied invalid header.
- [x] 2.3 Keep secrets and transport headers out of logs, telemetry, errors, prompts and responses. Use safe error codes; verify with synthetic markers across authentication/config/transport failures. Do not generate or log real keys in test evidence.

## 3. Frappe gateway and validated context

- [x] 3.1 Add non-guest whitelisted `ask()` with standard authentication/session CSRF handling, bounded question and existing optional session-ID validation. Construct the fixed `/orchestrate` envelope from authoritative Frappe user/site and `execution_scope="chat-only"`; ignore browser mode and refuse unsupported identity/scope/operation/target/URL overrides. Verify Guest refusal, malformed inputs, constructed payload and no arbitrary proxying.
- [x] 3.2 Derive mode from actual user roles against server-managed `nexmate_developer_roles`, default `[]`. No implicit System Manager or Administrator elevation; malformed mapping fails closed. Verify unmapped/admin defaults, explicit role match, ignored browser mode and identical chat-only guard for both personas. Mapping is not a corpus/tool permission grant.
- [x] 3.3 Read `nexmate_service_key` only from site configuration and the inference destination only from server configuration. Refuse missing/invalid Frappe key without development fallback; do not relay browser headers or follow credential-bearing redirects. Verify sanitized upstream failures and zero secret material in boot/client data. Desk must not depend on a browser-exposed inference address.
- [x] 3.4 Validate the complete gateway envelope before history/routing/retrieval: bounded nonempty user/site strings without edge whitespace/control characters, non-Guest user, supported mode, exact chat-only scope and exact match to trusted `NEXMATE_FRAPPE_SITE`. Require valid service authentication even in development. Verify partial/malformed envelopes, wrong/unconfigured site and unauthenticated attribution are refused without legacy downgrade. Only validated metadata enters attributed telemetry; no identity/headers added to prompts.
- [x] 3.5 Preserve valid optional `session_id` forwarding and existing JSON behavior without ownership claims. Verify unchanged identifier, absent/invalid cases and no migration or user/site ownership assignment. Record the required separately approved successor rather than implementing state binding here.

## 4. Deterministic backend chat-only guard

- [x] 4.1 Propagate immutable request-local empty tool allowance for gateway calls and enforce it before any code/file/read/search/explain/Git or ERPNext schema/document/list/write/proposal/apply/reset/approval invocation. Block `code`/`erpnext` handlers before entry, including troubleshooting, follow-ups, rewritten questions and degraded routing. Verify forced routes and natural-language tool requests in both personas with zero tool/client calls; UI/model-only denial does not pass.
- [x] 4.2 Suppress incidental live ERPNext version calls on all gateway chat paths, including RAG; return unavailable-version semantics without invented versions. Keep conversational/cited-RAG behavior and truthful chat-only capability help. Verify no incidental upstream calls or model-granted scope changes. Preserve existing legacy server/dev behavior behind authentication, not as Desk fallback; do not claim ACL retrieval or add corpus grants.

## 5. Desk boundary and standalone preview

- [x] 5.1 Move Desk chat to authenticated Frappe only, with no direct FastAPI fallback on failure. Disable misleading mode overrides and unavailable slash/tool handlers. Verify all Desk fetch paths, including error/retry paths, target Frappe only and carry no service key.
- [x] 5.2 Disable/refuse proposal/approval/rejection actions including stale cards; start-fresh clears local transcript and replaces the local session ID only, with no server reset/deletion claim. Verify these handlers issue zero inference/tool requests and do not report server decisions that never occurred.
- [x] 5.3 Keep standalone `/ui` as a distinct explicit dual-opt-in development workflow, never injecting a credential or relaxing Desk behavior. Verify the preview remains protected without both settings and that request/client-origin detection cannot activate backend exemption.

## 6. Future verification and rollback evidence

- [x] 6.1 Run the documented full Python suite (`python -m unittest discover -s tests`), applicable existing Node harness and new synthetic auth/gateway/dispatch tests during implementation, not this planning pass. Locate and run applicable configured lint/typecheck commands; per explicit 2026-09-17 user acceptance, Python lint, Python typecheck, JavaScript lint and JavaScript typecheck are each not configured, not passed. Do not request commands again or invent tooling for this acceptance. Record dates, environment, artifacts and limitations without equating unit/mock/stub-DOM evidence with real Bench acceptance.
- [x] 6.2 Verify the complete D2 matrix, all identity/site rejection cases, secret-safe failure paths and zero tool/client invocations for natural-language/forced gateway routes. Compare inference route inventory before/after: no existing endpoints removed and all protected paths gated. Use synthetic/local fixtures; no model/network/data calls are authorized by this planning pass.
- [ ] 6.3 Obtain bounded integration/Desk evidence in an explicitly available authorized environment: all Desk actions remain Frappe-only; preview uses only explicit development settings; minimal health works with key unset. Record unavailable Bench/live evidence as unverified, not passed. Any model/private-data or live-write work needs its separate standing approval; no production gate is implied.
- [x] 6.4 Review/probe rollback safely: retain inference gate and chat-only guard or disable Desk chat; no credential distribution, unauthenticated service rollback or production dev bypass. Verify rotation mismatch and gateway failure stay closed.

## 7. Future documentation and next milestone

- [x] 7.1 During implementation, update the development/onboarding documentation within its assigned scope with all configuration defaults, strong-key provisioning/rotation, minimal liveness versus readiness, single-project binding, dual preview opt-in and chat-only/legacy limitations. This planning pass does not edit those files.
- [x] 7.2 At implementation handoff, record dated evidence and remaining gates in progress files only when that scope is assigned; they are excluded from this planning pass. State explicitly that authentication groundwork is not complete G2/M2, owned state, ACL isolation, production readiness, release approval or private-data cloud consent.
- [x] 7.3 Per explicit 2026-09-17 user authorization, hand off ONLY despite pending Bench verification (6.3): the next milestone requires the separately approved `frappe-owned-conversation-state` change for Frappe-owned records, authenticated user ownership, site association, persistence across inference restarts, removal of caller-controlled session ownership, explicit migration/compatibility strategy and eventual removal of the JSON store. Do not create or implement that successor here; retain remaining U4 and other HLD questions for their affected gates.
