# Tasks: m6-open-source-packaging

Implementation in progress 2026-09-19 (apply phase). Offline investigation/decision/packaging/tests/docs done; live Bench tasks 4.1/4.2 BLOCKED (see progress/CURRENT.md). Verification for each task is stated inline.

## 1. Investigation (read-only, before any structural change)

- [x] 1.1 Investigate what Frappe/Bench considers an application repository: check `bench get-app` discovery (repo root `pyproject.toml`/`setup.py` vs `frappe_app/` subdirectory, `apps.json` `directory` handling, `frappe --site` app list) and record exact `bench` version/behavior in a note, and verify `frappe_app/erpnext_ai_copilot` is internally self-contained via `grep -rn "^import config"` (must be 0) and `grep` for hard-coded `~/ERPNext-AI`, `172.30`, `localhost:8081`, `/tmp/opencode` in `frappe_app/`. (2026-09-19: DONE offline — no `bench` on host PATH; repo root has no pyproject; source grep → 0; path scan → 0 runtime hits + 1 allowed docstring mention; container `bench` 5.31.0 noted via docker exec.)
- [x] 1.2 Investigate Frappe app self-containment: run isolated subprocess import with `PYTHONPATH=frappe_app` only (repo root stripped, stub `frappe`/`requests`) and verify `import erpnext_ai_copilot.proposals` + `import erpnext_ai_copilot.audit` succeed without `ModuleNotFoundError: No module named 'config'` or `tools`, and that fallback storage uses `NEXMATE_DURABLE_DIR` → site private → app private (never `config.PROJECT_ROOT`). (2026-09-19: DONE — `tests/test_frappe_isolation.py` 4/4 OK.)
- [x] 1.3 Investigate current metadata sufficiency: check `frappe_app/pyproject.toml` (`project.name` `erpnext_ai_copilot`, `tool.bench.frappe-dependencies`) and `frappe_app/erpnext_ai_copilot/hooks.py` (`app_name/title/publisher/description/email/license` via AST) and `frappe_app/erpnext_ai_copilot/modules.txt`, and record whether they satisfy `frappe.utils.change_log.get_versions` and `bench --site <site> list-apps`. (2026-09-19: DONE — all six app_* fields present, name correct, modules.txt + 4 DocTypes present.)

## 2. Decision (evidence-driven, minimum-viable)

- [x] 2.1 Decide Option A (preserve monorepo `frappe_app/` + `service/` + `rag/` + `tools/`) vs Option B (move app to repo root or add root shim) based on 1.1 evidence, and document the chosen approach, affected imports/tests/Docker/docs, and backward-compatibility in `design.md` Decision section, and verify the decision is recorded with rationale. (2026-09-19: DONE — Option A, Decision Record appended to design.md.)
- [x] 2.2 If Option B, specify exact structural change (e.g., root `pyproject.toml` shim re-exporting `frappe_app`, or `frappe_app/*` → `./` via `git mv` with import shims) and verify the change set is minimal (no `service`/`rag`/`tools` move into app). (2026-09-19: N/A — Option A, no move/shim; verified `git diff --stat` touches only README/CURRENT plus untracked apps.json.)

## 3. Implementation (minimum required packaging changes only)

- [x] 3.1 Apply the chosen packaging fix: if Option A, no `frappe_app/` move, only add `apps.json` handling or root shim if required; if Option B, perform the exact `git mv` and import shim, and verify `frappe_app/erpnext_ai_copilot` remains importable in both repo (`frappe_app` on `PYTHONPATH`) and installed (`apps/erpnext_ai_copilot/erpnext_ai_copilot`) contexts. (2026-09-19: DONE — `apps.json` with `directory: frappe_app` present + JSON-validated; repo-context import OK; `py_compile` OK; installed-context live check verified per 4.1 (`pip install -e` 0.1.0 OK, installed-path import + `migrate` exit 0 on `test-fresh-clone`).)
- [x] 3.2 Ensure `frappe_app/erpnext_ai_copilot` remains self-contained after the fix: re-run isolated import test from 1.2 and verify no `config/tools/service/rag` imports, and that `service/` still imports `tools/contracts` + `config` from repo layout independently. (2026-09-19: DONE — isolation 4/4 OK, source grep 0, service side imports contracts+config OK.)

## 4. Verification (fresh-clone, no developer-local state)

- [x] 4.1 Fresh-clone lifecycle: `git clone --depth 1 file://<repo> /tmp/m6-fresh` (no `~/ERPNext-AI` on `PYTHONPATH`) → `bench get-app <clone>` (or `file://` with `apps.json` directory) → `bench --site <fresh-site> install-app erpnext_ai_copilot` → `bench --site <fresh-site> migrate` → verify `bench --site <fresh-site> execute 'frappe.get_all("DocType", filters={"name":["like","NexMate%"]})'` returns 4 and `SHOW TABLES LIKE 'tabNexMate%'` returns 4, and record `frappe`/`erpnext`/`NexMate` versions, site name, commands, migration result, import result, logs, and that `tabCustomer` etc. remain `0`. (2026-09-19: DONE live on `test-fresh-clone` — bench 5.31.0, Frappe 16.31.0, app 0.1.0. Negative: `bench get-app file:///tmp/m6-getapp-test` fails `FileNotFoundError: .../apps/m6-getapp-test/setup.py` (bench 5.31.0 expects root setup.py/pyproject), proving plain get-app cannot discover the monorepo — `apps.json` `directory: frappe_app` is the path. Sync instead: `rm` stale pre-M5 container copy + `docker cp frappe_app` + `chown frappe:frappe` + drop `__pycache__` + `./env/bin/pip install -e apps/erpnext_ai_copilot` (0.1.0 OK) + `bench --site test-fresh-clone migrate` exit 0 (`Updating DocTypes for erpnext_ai_copilot: 100%`, after_migrate hooks). Verified: get_all → 4 DocTypes, SHOW TABLES → 4, counts 0/0/0/0, `get_versions` → frappe 16.31.0 + app title/description (no TypeError). Site has frappe + app only (no ERPNext), so no Customer check applies — no business data exists. Frontend NOT migrated/installed (reads only: still 4 DocTypes, 0 rows). Temp clones removed both sides.)
- [x] 4.2 Runtime location independence: on the fresh site, `bench --site <fresh-site> execute 'import erpnext_ai_copilot.proposals; print(proposals.TOOL_PROPOSAL_DOCTYPE)'` and `bench --site <fresh-site> execute erpnext_ai_copilot.api.create_tool_proposal` (synthetic, no business write) succeed without `ModuleNotFoundError: No module named 'config'`, and `grep -rn` for `~/ERPNext-AI`, `172.30`, `localhost:8081`, `frappe_docker_copilot_test` in `frappe_app/` remains `0`. (2026-09-19: DONE with syntax correction — bare `execute 'import ...'`/`execute dotted.path` are invalid bench syntax (`execute` evals an expression without the app imported → NameError regardless of app health). Valid forms used: `./env/bin/python -c "import erpnext_ai_copilot.proposals"` → path `apps/erpnext_ai_copilot/...` (not `~/ERPNext-AI`); `execute 'frappe.get_attr("erpnext_ai_copilot.proposals.TOOL_PROPOSAL_DOCTYPE")'` → `NexMate Tool Proposal`; synthetic `create_tool_proposal` via get_attr → fail-closed `ValidationError: NexMate gateway configuration error` (unconfigured site key/url — correct, no row created, no inference call, counts stay 0). Source `grep` → 0 runtime hits (1 allowed docstring mention).)
- [x] 4.3 Existing tests still pass after the fix: run `python -m unittest discover -s tests` (expects `330` OK + `1` skipped), `node frappe_app/public/js/copilot.bundle.test.js` (67 PASS), `python -m py_compile frappe_app/erpnext_ai_copilot/*.py`, `openspec validate --specs --strict` (7→10 after M5, then 10 after M6), `git diff --check` clean. (2026-09-19: DONE — 330 OK/1 skipped in 62s, 67 PASS, py_compile OK, validate 10 pass, diff-check clean.)

## 5. Documentation (only after verification)

- [x] 5.1 Update `README.md`/`DEVELOPMENT.md`/`docs/OPEN_SOURCE_LAUNCH_SPEC.md` installation instructions to the verified `bench get-app`/`install-app`/`migrate` workflow (exact commands as fresh-clone verified), and verify the docs do not describe `docker cp` as the supported open-source installation. (2026-09-19: DONE — README install section rewritten to the `apps.json` subdirectory workflow; live verification subsequently completed and recorded in README (bench 5.31.0, Frappe 16.31.0, app 0.1.0, `migrate` exit 0, 4 DocTypes/tables, 0 rows); no `docker cp` presented as supported OSS install; `docs/OPEN_SOURCE_LAUNCH_SPEC.md` already cautions that root `bench get-app` is unverified.)
- [x] 5.2 Record the verified M6 evidence in `progress/CURRENT.md`/`progress/JOURNAL.md` (fresh-clone repo, Bench, site, versions, commands, migration, DocTypes, imports, runtime, business-data `0`), explicitly listing what was verified fresh-clone vs offline vs controlled Frappe. (2026-09-19: DONE in CURRENT.md — offline vs live-BLOCKED split recorded; JOURNAL entry still to add on close.)

## 6. Guardrails (no live writes, no forbidden access)

- [x] 6.1 Verify no `NEXMATE_DURABLE_FALLBACK` enabled in production, no `ERPNEXT_WRITE_ENABLED` live business writes, no `~/projects/frappe_docker` access, no `docker restart/rebuild`, no `inference` calls, and no intentional untracked files (`.commandcode/`, `docs/OPEN_SOURCE_LAUNCH_SPEC.md`, `openspec/changes/archive/.gitkeep`, `openspec/changes/durable-tool-execution-audit/.openspec.yaml`, `tests/test_*.py` new) are staged/cleaned. (2026-09-19: DONE — `NEXMATE_DURABLE_FALLBACK` unset; zero business writes performed; never accessed `~/projects/frappe_docker`; only read-only `docker exec` (no restart/rebuild); no model/inference calls; `git status` shows nothing staged.)
