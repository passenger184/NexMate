[project]
name = "erpnext_ai_copilot"
authors = [
    { name = "ERPNext AI Copilot contributors" },
]
description = "Sidebar AI assistant for ERPNext/Frappe developer questions (Phase 1: pure RAG over public docs)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15"
