# progress/CURRENT.md — Current State

**Last updated:** (agent: update this timestamp every time you edit this file)
**Current phase:** Phase 1 — Pure RAG, Developer mode (see `docs/PHASE_1_SPEC.md`)
**Current task:** (agent: name the specific task in progress)

## What's built and verified

(agent: list concretely — e.g., "ingestion pipeline runs end-to-end on 20
sample docs, chunking verified manually" — not "ingestion done" alone)

## What's known to not work / not started yet

(agent: be specific and honest — this is what the user reads to know what
NOT to trust yet)

## Next step

(agent: the single next concrete action, so a resumed session — or the user
— knows exactly where to pick up)
